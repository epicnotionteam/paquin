{% comment %}
// Output Content [Offset, Careers] component
{% endcomment %}

{% comment %} Assign section data to vars {% endcomment %}
{% assign section_heading_small = section.settings.heading_small %}
{% assign section_heading_large = section.settings.heading_large %}
{% assign section_subheading    = section.settings.subheading %}
{% assign section_content       = section.settings.content %}
{% assign section_blocks        = section.blocks %}
{% assign section_theme         = section.settings.theme %}

{% comment %} Output section {% endcomment %}
<section class="section--content-offset variant--careers theme--{{ section_theme }}">
    {% comment %} Heading [Small] {% endcomment %}
    {% if section_heading_small != blank %}
        <h2 class="section__heading--small h5">{{ section_heading_small }}</h2>
    {% endif %}
    {% comment %} Heading [Large] {% endcomment %}
    {% if section_heading_large != blank %}
        <h3 class="section__heading--large h2">{{ section_heading_large }}</h3>
    {% endif %}
    {% comment %} Subheading {% endcomment %}
    {% if section_subheading != blank %}
        <p class="section__subheading">{{ section_subheading }}</p>
    {% endif %}

    {% comment %} Feed {% endcomment %}
    <div class="feed--role">
        {% for block in section_blocks %}
            {% comment %} Assign block data to vars {% endcomment %}
            {% assign block_title    = block.settings.title %}
            {% assign block_division = block.settings.division %}
            {% assign block_location = block.settings.location %}
            {% assign block_link     = block.settings.link %}
            {% comment %} Output item {% endcomment %}
            <div class="item">
                {% comment %} Container [Content] {% endcomment %}
                <div class="container--content">
                    {% comment %} Title {% endcomment %}
                    {% if block_title != blank %}
                        <p class="item__title">{{ block_title }}</p>
                    {% endif %}
                    {% comment %} Division {% endcomment %}
                    {% if block_division != blank %}
                        <p class="item__division">{{ block_division }}</p>
                    {% endif %}
                    {% comment %} Location {% endcomment %}
                    {% if block_location != blank %}
                        <p class="item__location">{{ block_location }}</p>
                    {% endif %}
                </div>
                {% comment %} Container [Link] {% endcomment %}
                <div class="container--link">
                    {% comment %} LinkedIn {% endcomment %}
                    {% if block_link != blank %}
                        <a href="{{ block_link }}" class="item__link link--arrow-outgoing" target="_blank">Apply</a>
                    {% endif %}
                </div>
            </div>
        {% endfor %}
    </div>
    {% comment %} Content {% endcomment %}
    {% if section_content != blank %}
        <div class="section__content">{{ section_content | richtext }}</div>
    {% endif %}
</section>

{% schema %}
{
    "name": "Content [Offset, Careers]",
    "settings": [
        {
            "type": "text",
            "id": "heading_small",
            "label": "Heading [Small]",
            "default": "Heading",
            "info": "Primary section heading (h2)"
        },
        {
            "type": "text",
            "id": "heading_large",
            "label": "Heading [Large]",
            "default": "Heading",
            "info": "Secondary section heading (h3)"
        },
        {
            "type": "text",
            "id": "subheading",
            "label": "Subheading",
            "default": "Subheading"
        },
        {
            "type": "richtext",
            "id": "content",
            "label": "Content",
            "default": "<p>Content</p>"
        },
        {
            "type": "select",
            "id": "theme",
            "label": "Section Theme",
            "options": [
              {
                "value": "base",
                "label": "Base"
              },
              {
                "value": "alt",
                "label": "Alternative"
              },
              {
                "value": "dark",
                "label": "Dark"
              }
            ],
            "default": "base"
        }
    ],
    "blocks": [
        {
            "type": "role-block",
            "name": "Role",
            "settings": [
                {
                    "type": "text",
                    "id": "title",
                    "label": "Title",
                    "default": "Title"
                },
                {
                    "type": "text",
                    "id": "division",
                    "label": "Division",
                    "default": "Division"
                },
                {
                    "type": "text",
                    "id": "location",
                    "label": "Location",
                    "default": "Location"
                },
                {
                    "type": "url",
                    "id": "link",
                    "label": "Job Post URL"
                }
            ]
        }
    ],
    "presets": [
        {
            "name": "Content [Offset, Careers]",
            "category": "Paquin"
        }
    ]
}
{% endschema %}